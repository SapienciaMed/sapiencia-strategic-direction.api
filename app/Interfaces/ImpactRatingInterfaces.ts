export interface IImpactRating {
  id: number,
  description: string,
  active: boolean,
  order: number
}

export interface IImpactRatingTemp {
  id?: number,
  description: string,
  active: boolean,
  order: number
}
